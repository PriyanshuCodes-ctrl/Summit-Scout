# Summit Scout — GitHub Upload Version

This version is intentionally FLAT to match a GitHub repository where all files are in the repository root.

Keep `index.html`, `style.css`, all `.js` files, and the other `.html` pages together in the same root folder.

For GitHub Pages: Settings → Pages → Deploy from a branch → main → / (root).
